<!DOCTYPE hibernate-configuration PUBLIC
  "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
  "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
  <session-factory>
     <!-- Database connection settings -->
        <property name="connection.driver_class">org.hsqldb.jdbcDriver</property>
        <property name="connection.url">jdbc:hsqldb:file:data/collpro.dat</property>
        <property name="hibernate.connection.shutdown">true</property>
        <property name="connection.username">sa</property>
        <property name="connection.password"></property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">2</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.HSQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup --> 
        <!-- <property name="hbm2ddl.auto">create</property -->
        <property name="hbm2ddl.auto">update</property>

        <mapping class="com.petpet.collpro.datamodel.Characteristic"/>
        <mapping class="com.petpet.collpro.datamodel.StringCharacteristic"/>
        <mapping class="com.petpet.collpro.datamodel.Element"/>
  </session-factory>
</hibernate-configuration>
